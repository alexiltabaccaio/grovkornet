material {
    name : "CompositeShader",
    shadingModel : unlit,
    parameters : [
        { type : sampler2d, name : u_Texture },
        { type : sampler2d, name : u_BloomTexture },
        { type : float, name : u_GrainIntensity },
        { type : float, name : u_GrainChroma },
        { type : float, name : u_GrainSize },
        { type : float, name : u_GrainSpeed },
        { type : float, name : u_GrainRoughness },
        { type : float, name : u_VignetteIntensity },
        { type : float, name : u_VhsIntensity },
        { type : float, name : u_Time },
        { type : float, name : u_BloomIntensity },
        { type : float, name : u_ChromaticAberration },
        { type : float, name : u_AberrationDirection },
        { type : float, name : u_InvertYShift },
        { type : float, name : u_AberrationInvert },
        { type : sampler2d, name : u_OverlayTexture },
        { type : float, name : u_OverlayEnabled },
        { type : float, name : u_DrsScale },
        { type : float, name : u_Sharpening },
        { type : float2, name : u_TexelSize },
        { type : float, name : u_TargetResolution },
        { type : sampler2d, name : u_GlassTexture },
        { type : float, name : u_PixelationFactor },
        { type : float, name : u_PanelY }
    ],
    requires : [ uv0 ]
}

fragment {
    highp float rand(highp vec2 co) {
        return fract(sin(dot(co, vec2(12.9898, 78.233))) * 43758.5453);
    }
    highp float rand2(highp vec2 co) {
        return fract(sin(dot(co, vec2(39.346, 11.135))) * 43758.5453);
    }
    highp float rand3(highp vec2 co) {
        return fract(sin(dot(co, vec2(73.156, 52.235))) * 43758.5453);
    }
    void material(inout MaterialInputs material) {
        prepareMaterial(material);
        vec2 uv = getUV0();
        
        if (materialParams.u_TargetResolution > 0.0) {
            float minDim = min(1.0 / materialParams.u_TexelSize.x, 1.0 / materialParams.u_TexelSize.y);
            if (materialParams.u_TargetResolution < minDim) {
                float screenW = 1.0 / materialParams.u_TexelSize.x;
                float screenH = 1.0 / materialParams.u_TexelSize.y;
                float scale = materialParams.u_TargetResolution / minDim;
                vec2 gridSize = vec2(screenW * scale, screenH * scale);
                uv = (floor(uv * gridSize) + 0.5) / gridSize;
            }
        }

        if (materialParams.u_PixelationFactor > 1.0) {
            float minDim = min(1.0 / materialParams.u_TexelSize.x, 1.0 / materialParams.u_TexelSize.y);
            float scaledFactor = max(1.0, materialParams.u_PixelationFactor * (minDim / 1080.0));
            vec2 gridSize = vec2(1.0 / materialParams.u_TexelSize.x, 1.0 / materialParams.u_TexelSize.y) / scaledFactor;
            uv = (floor(uv * gridSize) + 0.5) / gridSize;
        }

        vec2 baseUv = uv * materialParams.u_DrsScale;
        vec2 compositeUv = baseUv;
        vec4 baseColor;
        
        vec2 rUv = compositeUv;
        vec2 gUv = compositeUv;
        vec2 bUv = compositeUv;

        if (materialParams.u_VhsIntensity > 0.0) {
            float jitter = sin(uv.y * 50.0 + materialParams.u_Time * 10.0) * 0.002 * materialParams.u_VhsIntensity;
            compositeUv.x += jitter;
            
            float shift = 0.005 * materialParams.u_VhsIntensity;
            rUv = vec2(compositeUv.x - shift, compositeUv.y);
            gUv = compositeUv;
            bUv = vec2(compositeUv.x + shift, compositeUv.y);
        }

        if (materialParams.u_ChromaticAberration > 0.0) {
            float caIntensity = materialParams.u_ChromaticAberration * 0.02;
            vec2 caShift = vec2(0.0);
            float aspect = materialParams.u_TexelSize.y / materialParams.u_TexelSize.x;
            
            if (materialParams.u_AberrationDirection < 0.5) {
                caShift = vec2(0.0, caIntensity * aspect);
            } else if (materialParams.u_AberrationDirection < 1.5) {
                caShift = vec2(caIntensity, 0.0);
            } else {
                vec2 dir = normalize(uv - 0.5);
                float dist = length(uv - 0.5);
                caShift = dir * dist * 2.0 * caIntensity;
                caShift.y *= aspect;
            }
            
            if (materialParams.u_InvertYShift > 0.5) {
                caShift.y = -caShift.y;
            }

            if (materialParams.u_AberrationInvert > 0.5) {
                caShift = -caShift;
            }

            rUv -= caShift;
            bUv += caShift;
        }

        vec4 colR = texture(materialParams_u_Texture, rUv);
        vec4 colG = texture(materialParams_u_Texture, gUv);
        vec4 colB = texture(materialParams_u_Texture, bUv);
        baseColor = vec4(colR.r, colG.g, colB.b, colG.a);

        if (materialParams.u_Sharpening > 0.0) {
            vec2 texel = materialParams.u_TexelSize;
            vec3 colN = textureLod(materialParams_u_Texture, compositeUv + vec2(0.0, texel.y), 0.0).rgb;
            vec3 colS = textureLod(materialParams_u_Texture, compositeUv - vec2(0.0, texel.y), 0.0).rgb;
            vec3 colE = textureLod(materialParams_u_Texture, compositeUv + vec2(texel.x, 0.0), 0.0).rgb;
            vec3 colW = textureLod(materialParams_u_Texture, compositeUv - vec2(texel.x, 0.0), 0.0).rgb;
            
            vec3 laplacian = 4.0 * baseColor.rgb - (colN + colS + colE + colW);
            baseColor.rgb += laplacian * materialParams.u_Sharpening * 10.0;
        }

        if (materialParams.u_VhsIntensity > 0.0) {
            // Scanlines
            float scanline = sin(uv.y * 800.0) * 0.04 * materialParams.u_VhsIntensity;
            baseColor.rgb -= vec3(scanline);
        }
        
        // Fix for inverted bloom reflection: flip the V coordinate within the valid DRS range
        vec2 flippedBloomUv = vec2(compositeUv.x, materialParams.u_DrsScale - compositeUv.y);
        vec3 bloomColor = texture(materialParams_u_BloomTexture, flippedBloomUv).rgb;
        
        // Soft additive blend in linear space
        vec3 finalColor = baseColor.rgb + bloomColor * materialParams.u_BloomIntensity;
        
        // Vignette
        if (materialParams.u_VignetteIntensity > 0.0) {
            vec2 d = abs(uv - 0.5) * materialParams.u_VignetteIntensity;
            float vignette = 1.0 - dot(d, d);
            finalColor *= clamp(vignette, 0.0, 1.0);
        }
        
        // Grain (procedural)
        if (materialParams.u_GrainIntensity > 0.0) {
            highp float size = materialParams.u_GrainSize;
            highp vec2 grainUv = floor(uv * (1080.0 / clamp(size, 0.1, 10.0))) * clamp(size, 0.1, 10.0) / 1080.0;
            
            highp float randR = rand(grainUv);
            highp float randG = rand2(grainUv);
            highp float randB = rand3(grainUv);
            highp float t = materialParams.u_Time * materialParams.u_GrainSpeed;
            
            highp float noiseR = fract(randR + t * 0.11);
            highp float noiseG = fract(randG + t * 0.19);
            highp float noiseB = fract(randB + t * 0.33);
            
            highp vec3 noise = vec3(noiseR, noiseG, noiseB);
            
            // MONO: custom speeds to match RGB perceived speed (around 0.18) but slightly detuned to prevent Moiré circles
            highp float monoFastR = fract(randR + t * 0.171);
            highp float monoFastG = fract(randG + t * 0.180);
            highp float monoFastB = fract(randB + t * 0.189);
            
            // MONO: soft (bell curve average - 0.5) vs rough (single channel - 0.5)
            highp float softMono = (monoFastR + monoFastG + monoFastB) / 3.0 - 0.5;
            highp float roughMono = monoFastB - 0.5;
            highp float finalMono = mix(softMono, roughMono, materialParams.u_GrainRoughness);
            
            // RGB: soft (balanced chroma scaled by 1/sqrt(3)) vs rough (pure chroma)
            highp vec3 softRgb = (noise - 0.5) * 0.577350269;
            highp vec3 roughRgb = noise - 0.5;
            highp vec3 finalRgb = mix(softRgb, roughRgb, materialParams.u_GrainRoughness);
            
            // Blend MONO and RGB based on u_GrainChroma
            highp vec3 finalNoise = mix(vec3(finalMono), finalRgb, materialParams.u_GrainChroma);
            
            finalColor += finalNoise * materialParams.u_GrainIntensity;
        }
        
        // Overlay Texture (Dust & Scratch)
        if (materialParams.u_OverlayEnabled > 0.0) {
            vec4 overlay = textureLod(materialParams_u_OverlayTexture, uv, 0.0);
            finalColor = mix(finalColor, overlay.rgb, overlay.a);
        }
        
        // Glassmorphism blur overlay
        if (materialParams.u_PanelY < 1.0) {
            // Sample the pre-blurred glass texture
            vec3 glassColor = texture(materialParams_u_GlassTexture, flippedBloomUv).rgb;
            
            // Tinting is now delegated to the React Native UI layer across the full screen width
            vec3 tintedGlass = glassColor;
            
            // Panel top Y coordinate (0.0 is top, 1.0 is bottom in Android Surface UV space)
            float panelTopY = materialParams.u_PanelY;
            
            // Antialiased edge transition: 1.0 for uv.y > panelTopY, 0.0 for uv.y < panelTopY
            float edgeSoftness = 0.002;
            float insidePanel = smoothstep(panelTopY - edgeSoftness, panelTopY + edgeSoftness, uv.y);
            
            finalColor = mix(finalColor, tintedGlass, insidePanel);
        }
        
        material.baseColor = vec4(finalColor, baseColor.a);
    }
}
